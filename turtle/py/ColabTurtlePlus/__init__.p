"""ColabTurtlePlus — Canvas-based turtle for Pyodide."""
from .Turtle import (
    setup, showborder, bgcolor, speed,
    hideturtle, showturtle, ht, st,
    penup, pendown, pu, pd, up, down,
    pencolor, fillcolor, color,
    pensize, width,
    goto, setpos, setposition,
    forward, fd, backward, bk, back,
    left, lt, right, rt,
    setheading, seth,
    home,
    pos, position, xcor, ycor, heading, distance,
    circle, dot,
    begin_fill, end_fill,
    write,
    clearscreen, reset, done,
    window_width, window_height,
)
 
